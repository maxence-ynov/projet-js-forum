services:
  forum:
    build: .
    image: forum:latest
    ports:
      - "8080:8080"
    environment:
      - FORUM_DB_PATH=/data/forum.db
    volumes:
      - ./data:/data
      - ./static:/app/static:ro
      - ./templates:/app/templates:ro
    restart: unless-stopped
